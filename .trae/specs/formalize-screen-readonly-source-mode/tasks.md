# Tasks
- [x] Task 1: 固化天猫专享正式页面只读模式的配置边界
  - [x] SubTask 1.1: 明确任务配置中采集方式字段如何表达 `页面只读` 与 `OCR识别`
  - [x] SubTask 1.2: 明确天猫专享页面只读模式的等待中、已就绪、读取成功、读取失败状态口径
  - [x] SubTask 1.3: 明确天猫专享页面只读正式值写入任务运行态后，与 OCR 值的互斥关系和展示口径

- [x] Task 2: 补任务配置与后端模型的正式支持
  - [x] SubTask 2.1: 为任务模型、保存接口和默认值增加正式采集方式字段
  - [x] SubTask 2.2: 让任务详情、任务快照和店铺卡片相关接口都能返回当前采集方式与数据来源
  - [x] SubTask 2.3: 保证老任务未设置时仍按原有 OCR 默认行为运行

- [x] Task 3: 将天猫专享页面只读接入正式调度采集链路
  - [x] SubTask 3.1: 在调度器中为 `页面只读` 任务增加独立采样逻辑，读取天猫 `screen.htm` 的 `payAmt.value`
  - [x] SubTask 3.2: 当页面未进入大屏时，任务进入等待/未就绪状态并持续重试
  - [x] SubTask 3.3: 页面只读读取成功后，更新正式最新值、采样时间、状态与原因说明

- [x] Task 4: 更新采集配置 UI 与实时看板展示
  - [x] SubTask 4.1: 在采集配置中把采集方式改成正式可选项：`页面只读`、`OCR识别`
  - [x] SubTask 4.2: 在页面只读模式下保留必要的测试信息，同时让用户能保存为正式任务配置
  - [x] SubTask 4.3: 在实时看板店铺卡片或合适位置展示当前值来源，确保页面只读任务的正式值真正落到卡片中

- [x] Task 5: 做正式链路专项验证
  - [x] SubTask 5.1: 验证 `页面只读` 任务在已进入大屏时能持续更新实时看板卡片数值
  - [x] SubTask 5.2: 验证 `页面只读` 任务在未进入大屏时进入等待态，进入大屏后自动恢复正常采集
  - [x] SubTask 5.3: 验证 `OCR识别` 任务仍按原有方式工作，未被页面只读模式破坏
  - [x] SubTask 5.4: 验证切换采集方式后，保存配置、任务快照、实时看板和状态说明保持一致

# Task Dependencies
- Task 2 depends on Task 1
- Task 3 depends on Task 1 and Task 2
- Task 4 depends on Task 2 and Task 3
- Task 5 depends on Task 3 and Task 4
