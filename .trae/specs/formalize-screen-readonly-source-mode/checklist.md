- [x] 采集配置中可正式选择天猫专享 `页面只读` 或 `OCR识别`
- [x] 老任务未设置采集方式时仍按原有 OCR 默认行为运行
- [x] 天猫专享页面只读任务在已进入大屏时能把 `payAmt.value` 写入正式任务运行态
- [x] 天猫专享页面只读任务在未进入大屏时会显示等待/未就绪状态，并在进入大屏后自动恢复正常采集
- [x] 实时看板店铺卡片展示的是页面只读任务的正式最新值，而不是测试面板临时值
- [x] 任务快照、状态说明或卡片上能识别当前值来源于 `页面只读` 或 `OCR识别`
- [x] `OCR识别` 任务的预览、OCR 测试、保存配置和正式采集链路未被破坏
- [x] 页面只读正式模式仍只在真实页面上下文中读取数据，没有引入浏览器外接口重放
