# Tasks
- [x] Task 1: 深度评估步骤1现状与最新店铺配置映射边界
  - [x] SubTask 1.1: 梳理当前步骤 1 会话下拉框的数据来源（`edge_sessions`、任务 `edge_session_id`、`/api/shops`）与默认值逻辑。
  - [x] SubTask 1.2: 明确“最新版店铺配置”对应的会话命名与排序规则，确认当前截图中的旧值来自哪里。
  - [x] SubTask 1.3: 锁定兼容边界：不破坏已保存任务、不影响任务管理与 OCR 主链路。

- [x] Task 2: 实现步骤1按最新店铺配置收敛候选会话
  - [x] SubTask 2.1: 调整候选会话生成逻辑，让当前任务优先匹配最新店铺配置对应会话。
  - [x] SubTask 2.2: 对不在最新店铺配置中的历史会话做隐藏、降级或明确提示，避免继续作为主候选误导用户。
  - [x] SubTask 2.3: 在步骤 1 上下文区域展示“当前默认会话来源/是否与最新店铺配置一致”的说明。

- [x] Task 3: 保持旧任务兼容与受控回退
  - [x] SubTask 3.1: 若任务仍绑定历史会话，保留可查看/可重绑/可恢复路径，不强制立即切换。
  - [x] SubTask 3.2: 确保保存、重绑、预览、测试 OCR 使用的是用户最终确认后的会话与页面。

- [x] Task 4: 冒烟测试与回归验证
  - [x] SubTask 4.1: 场景 A：当前任务对应最新店铺配置时，步骤 1 默认显示正确会话。
  - [x] SubTask 4.2: 场景 B：存在历史遗留会话时，步骤 1 不再把它当作当前店铺主候选。
  - [x] SubTask 4.3: 场景 C：旧任务仍绑定历史会话时，可继续进入重绑与恢复流程。
  - [x] SubTask 4.4: 场景 D：步骤 1 选择会话后，扫描页签、绑定页面、生成预览、测试 OCR、保存任务链路正常。
  - [x] SubTask 4.5: 执行诊断检查，确认无新增明显错误。

# Task Dependencies
- Task 2 depends on Task 1
- Task 3 depends on Task 2
- Task 4 depends on Task 2 and Task 3
