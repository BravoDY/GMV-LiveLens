- [x] 抖音在前端配置、后端只读接口、正式调度采集三层都被一致识别为受支持的 `screen_readonly` 平台
- [x] 抖音单店大屏只读仅读取 `今日用户支付金额(含异常交易)`，不会误读相邻其它金额指标
- [x] 抖音只读接口返回结构与天猫、京东、唯品会保持统一字段和统一状态语义
- [x] 抖音大屏只读未就绪时返回抖音专属等待态或失败态 reason code，而不是复用不准确的其它平台提示
- [x] 抖音 `screen_readonly` 正式采集轮询频率收口为 `1 秒`
- [x] 前端 WebSocket 失败后的全局 HTTP 兜底轮询未被误改成抖音专属逻辑
- [x] 抖音任务保存为 `screen_readonly` 后，`capture-once` 与正式调度链路都能把正式值写入任务运行态
- [x] 不再出现“只读测试接口支持抖音，但正式采集仍提示平台未配置”的层间不一致
- [x] 天猫、京东、唯品会现有大屏只读链路与状态展示未被抖音规范收口破坏
