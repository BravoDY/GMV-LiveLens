- [x] `ShopConfig`、`/api/shops` 与前端 `liveTasks()` 已能稳定透传 `brand`
- [x] 实时看板顶部仍保留全渠道总卡与平台汇总卡
- [x] 实时看板下半区已从单一店铺网格改为左右两个品牌面板
- [x] 左侧区域标题显示 `大货独立店`，右侧区域标题显示 `子品牌独立店`，且标题直接使用 `brand` 原文
- [x] 店铺只根据 `shops.csv` 的 `brand` 分区，不再按店铺名称推断
- [x] `brand` 缺失或非法值时，店铺默认归入左侧而不会丢失
- [x] 每个品牌区域桌面态每行最多显示 2 张店铺卡，并具备窄屏响应式降级
- [x] 店铺卡保留平台角标、平台色、GMV、目标、达成进度、进度条、更新时间
- [x] 页面不渲染任何“预留位 / 新增店铺”占位卡
- [x] 顶部闪光、数字高亮、异常态边框与 WebSocket 刷新行为未被破坏
- [x] 相关文件无新增诊断错误
