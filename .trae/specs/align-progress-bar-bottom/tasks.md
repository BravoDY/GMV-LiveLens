# Tasks

- [x] Task 1: `.total-card-bottom` / `.platform-card-bottom` 改为 flex-column + progress-track `margin-top: auto`
  - [x] `.total-card-bottom` 增加 `display: flex; flex-direction: column`
  - [x] `.platform-card-bottom` 增加 `display: flex; flex-direction: column`
  - [x] `.total-progress-track` 的 `margin-top: 10px` 改为 `margin-top: auto`
  - [x] `.platform-progress-track` 增加 `margin-top: auto` 和 `overflow: hidden`
  - [x] 刷新页面验证两卡进度条底边对齐

# Task Dependencies
- 无依赖，单文件单任务
