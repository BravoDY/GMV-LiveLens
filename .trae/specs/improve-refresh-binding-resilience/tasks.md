# Tasks
- [x] Task 1: 深度评估与改造边界确认（不破坏现网行为）
  - [x] SubTask 1.1: 梳理当前“刷新 -> 拉任务 -> 拉候选页 -> 判定重绑”的完整时序与失败分支。
  - [x] SubTask 1.2: 明确高风险点与保护策略（仅唯一候选自动改绑、失败回退人工重绑、保留旧入口）。
  - [x] SubTask 1.3: 形成可执行变更清单并锁定“不做项”（不改 OCR 主链路、不改调度间隔、不改数据库历史数据）。

- [x] Task 2: 后端实现绑定自愈增强（保守改造）
  - [x] SubTask 2.1: 在任务读取/候选页接口链路增加轻量 `reconcile + auto_restore` 调用点，避免刷新后立即误判失效。
  - [x] SubTask 2.2: 强化自动恢复结果输出（恢复成功/失败原因、是否需人工确认）以支撑前端准确提示。
  - [x] SubTask 2.3: 确保仅“唯一高置信候选”触发自动改绑，其余场景保持手动确认。

- [x] Task 3: 前端实现刷新后上下文回填与无感续接
  - [x] SubTask 3.1: 持久化最小必要上下文（当前配置任务 ID、最近绑定会话 ID）。
  - [x] SubTask 3.2: 页面刷新后自动恢复上下文并触发候选页预加载，优先展示恢复后的绑定态。
  - [x] SubTask 3.3: 对“恢复失败/不确定”状态给出明确可操作提示，并保留原重绑按钮与流程。

- [x] Task 4: 全链路冒烟测试与回归验证
  - [x] SubTask 4.1: 场景 A：正常刷新（页签未变）应自动恢复，不需手动重绑。
  - [x] SubTask 4.2: 场景 B：`page_id` 失效但 URL 唯一可匹配，应自动重绑成功。
  - [x] SubTask 4.3: 场景 C：多候选歧义，应拒绝自动改绑并提示人工确认。
  - [x] SubTask 4.4: 场景 D：Edge 调试链路短暂断开后恢复，应回到可采集状态。
  - [x] SubTask 4.5: 场景 E：调度采集与 OCR 链路持续可用（无功能回退）。
  - [x] SubTask 4.6: 执行诊断与关键日志检查，确认无新增明显错误。

# Task Dependencies
- Task 2 depends on Task 1
- Task 3 depends on Task 2
- Task 4 depends on Task 2 and Task 3
