- [x] 刷新页面后，若目标页签仍可唯一识别，系统可自动恢复绑定且无需手动重绑
- [x] 当 `page_id` 失效但候选唯一时，系统可自动重挂并更新任务绑定字段
- [x] 当候选不唯一时，系统不会盲目自动改绑，并明确提示人工确认
- [x] 前端刷新后可恢复最小上下文（当前任务、最近会话）并自动回填绑定流程
- [x] 现有手动重绑入口保持可用，失败时可回退到旧流程
- [x] 调度采集/OCR/任务管理主链路在改造后可正常运行
- [x] 完成场景化冒烟测试（A~E）并记录通过结果
- [x] 相关改动文件诊断无新增明显错误
