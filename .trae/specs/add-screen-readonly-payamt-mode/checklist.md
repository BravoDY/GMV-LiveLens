- [x] 采集配置工作台提供了手动开启的大屏只读模式入口
- [x] 系统仅在真实页面上下文中读取 `screen.htm` 对应的 `payAmt.value`
- [x] 页面中能看到最新 `payAmt`、最近更新时间和最近若干条测试记录
- [x] 未进入大屏、页面失效或读取失败时，页面会显示明确提示
- [x] 大屏只读模式测试结果不会写入正式采集结果，也不会覆盖 OCR 结果
- [x] 开启大屏只读模式后，现有 OCR 预览、OCR 测试、保存配置链路仍可正常使用
