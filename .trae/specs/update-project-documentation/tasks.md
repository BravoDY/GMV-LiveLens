# Tasks

- [x] Task 1: 更新全局配置与 OCR 引擎章节
  - [x] 在说明书中补充“全局 OCR 引擎切换”功能，特别说明 `ddddocr` 的应用场景（艺术字/高干扰）及正则表达式后置过滤（如过滤“羊”字）。
  - [x] 补充“全局采集频率”设置（默认 0.5 秒），说明其替代了单任务独立频率。
- [x] Task 2: 补充自动样本收集与容错机制章节
  - [x] 新增“自动 OCR 样本收集”章节，说明 `data/ocr_datasets/` 目录的生成规则（`平台_店铺_识别结果_时间戳.png`）及其对未来微调模型的意义。
  - [x] 在 Edge 会话/故障排查部分，补充 `page.screenshot` 5 秒超时降级策略（移除 `animations="disabled"`）。
- [x] Task 3: 更新页面绑定规则与状态机逻辑
  - [x] 更新页面绑定流程描述，明确系统已放宽校验，任何页签均可绑定并直接进入“生成预览”与采集状态，后端统一视为 `edge_target_page_ready`。
  - [x] 更新连续确认逻辑（`_judge`）：明确“下跌即预警”，且跳变（下跌或 >5 倍暴增）时需 `max(3, confirm_count + 1)` 次连续确认才能自愈的机制。