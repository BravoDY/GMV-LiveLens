- [x] 说明书中包含了 `ddddocr` 引擎及正则表达式清洗的描述。
- [x] 说明书中包含了全局采集频率（如 0.5s）的描述。
- [x] 说明书中包含了 `data/ocr_datasets/` 自动样本收集机制的描述。
- [x] 说明书中包含了 `page.screenshot` 超时降级（重试时移除动画限制）的描述。
- [x] 说明书中明确了放宽的页面绑定规则（任何页签可绑，统一视作 ready）。
- [x] 说明书中的连续确认逻辑已更新，包含下跌必预警及 `max(3, confirm_count + 1)` 次自愈机制。