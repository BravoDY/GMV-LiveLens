# Tasks
- [x] Task 1: 复盘刷新后误判失效的完整链路
  - [x] SubTask 1.1: 梳理“浏览器刷新 -> 恢复最小上下文 -> 拉任务/候选页 -> 步骤 1 判定”的完整时序。
  - [x] SubTask 1.2: 明确哪些状态字段仍然只依赖运行时 `page_id`，哪些字段已经具备跨刷新恢复能力。
  - [x] SubTask 1.3: 归纳本次真实回归场景与旧规格未覆盖的缺口，形成不破坏现有链路的修复边界。

- [x] Task 2: 后端补强跨刷新重挂策略
  - [x] SubTask 2.1: 评估并补齐绑定持久化所需的稳定识别信号，避免只靠 `page_id`。
  - [x] SubTask 2.2: 在刷新恢复链路中优先基于当前店铺会话和稳定指纹做可控重挂。
  - [x] SubTask 2.3: 为“恢复成功 / 候选歧义 / 真实失效 / 会话未就绪”补充明确 reason_code 或说明字段。

- [x] Task 3: 前端补强刷新后的状态机与文案
  - [x] SubTask 3.1: 页面刷新后先展示“恢复中/校验中”，不要在恢复完成前直接降级成“绑定页签已失效”。
  - [x] SubTask 3.2: 让最小上下文恢复、候选页预加载、预览入口共用同一份“最新绑定上下文”。
  - [x] SubTask 3.3: 仅在后端确认真实失效后，才展示“重新选择页面/页签失效”卡片。

- [x] Task 4: 做浏览器刷新专项真实冒烟
  - [x] SubTask 4.1: 场景 A：前端页面刷新，但真实业务页没关，系统自动恢复到“已选中/可生成预览”。
  - [x] SubTask 4.2: 场景 B：刷新导致 `page_id` 变化，但 URL/标题仍能唯一识别，系统自动重挂成功。
  - [x] SubTask 4.3: 场景 C：当前店铺会话存在多个相似候选页，系统进入人工确认，不误报真实失效。
  - [x] SubTask 4.4: 场景 D：真实业务页已关闭或已离开目标页，系统才显示“绑定页签已失效”。
  - [x] SubTask 4.5: 场景 E：刷新后立即点击“生成预览”，不会再次因为旧上下文回退失效。

# Task Dependencies
- Task 2 depends on Task 1
- Task 3 depends on Task 1 and Task 2
- Task 4 depends on Task 2 and Task 3
