- [x] 浏览器刷新后，若真实业务页仍在当前店铺会话中，步骤 1 不会先误报“绑定页签已失效”
- [x] 当 `page_id` 因刷新变化但稳定指纹仍能唯一命中时，系统可自动恢复绑定
- [x] 恢复中的中间态与“真实失效”在 UI 上可明确区分
- [x] 候选歧义时进入人工确认，而不是误判为真实失效
- [x] 刷新后立即点击“生成预览”，读取的是恢复后的最新绑定上下文
- [x] 真实业务页被关闭或离开目标页时，系统才显示“需重新选择页面/绑定页签已失效”
- [x] 多店铺刷新专项冒烟通过，且未破坏现有扫描、预览、OCR、保存链路
