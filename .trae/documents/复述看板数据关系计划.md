# 复述看板数据关系计划

## Summary

- 目标：基于当前仓库中的真实 CSV 文件与后端读取逻辑，准确复述 `shops_name.csv`、`shops.csv`、`target.csv`、`to_date.csv` 以及 MySQL 数据库在看板中的关系。
- 输出形式：以“关系说明”为主，强调每个文件的职责、主连接键、数据流向和在实时/周期看板中的作用。
- 范围：只做结构化复述，不修改代码、不扩展实现方案、不推导超出当前文件与已有对话之外的新业务规则。

## Current State Analysis

### 已完成的只读核对

- 已读取以下真实数据文件表头与示例内容：
  - `c:\Users\yjd22\Desktop\python项目\GMV-LiveLens\data\shops_name.csv`
  - `c:\Users\yjd22\Desktop\python项目\GMV-LiveLens\data\shops.csv`
  - `c:\Users\yjd22\Desktop\python项目\GMV-LiveLens\data\target.csv`
  - `c:\Users\yjd22\Desktop\python项目\GMV-LiveLens\data\to_date.csv`
- 已读取当前后端数据集与查询逻辑：
  - `c:\Users\yjd22\Desktop\python项目\GMV-LiveLens\backend\services\dashboard_dataset.py`
  - `c:\Users\yjd22\Desktop\python项目\GMV-LiveLens\backend\services\dashboard_query.py`

### 已确认的真实字段

- `shops_name.csv`
  - 字段：`companyshop_name`
- `shops.csv`
  - 关键字段：`platform`、`companyshop_name`、`brand`、`shop_name`、`default_page_url`、`enabled`、`Target`
- `target.csv`
  - 字段：`date`、`companyshop_name`、`target`
- `to_date.csv`
  - 字段：`product`、`chinese_product`、`date`、`to_date`

### 从当前代码确认的关系

- `dashboard_dataset.py` 当前负责：
  - 读取 `to_date.csv` 并按 `chinese_product` 分组形成周期数据集
  - 固定补一个 `realtime` 数据集
  - 读取 `target.csv` 作为目标数据
  - 读取 `shops_name.csv` 作为店铺主名称列表
- `dashboard_query.py` 当前负责：
  - `realtime`：从任务快照取实时 GMV
  - `period`：按 `to_date.csv` 给出的日期范围查 MySQL，并结合 `target.csv` 汇总目标
  - MySQL 查询表：`descente_al店铺整体取数源`
  - 查询字段：`平台`、`统计日期`、`支付金额`

## Proposed Changes

### 本次交付内容

- 用清晰中文复述 5 个数据源/配置项在看板中的角色：
  - `shops_name.csv`
  - `shops.csv`
  - `target.csv`
  - `to_date.csv`
  - MySQL 数据库表
- 说明它们之间的主关系：
  - 哪个是主店铺底表
  - 哪个控制导航
  - 哪个提供目标
  - 哪个提供实时采集配置
  - 哪个提供历史单日 GMV
- 说明它们如何共同支撑两类页面：
  - 实时看板
  - 周期切换看板

### 输出组织方式

- 第一部分：逐个文件说明职责。
- 第二部分：说明它们之间的连接关系。
- 第三部分：说明在“实时”与“周期”两种看板模式下，各自如何参与。
- 第四部分：补充当前代码里已经体现出的对应落点，但不展开代码实现细节。

## Assumptions & Decisions

- 以当前仓库真实 CSV 表头和后端读取逻辑为依据，不再重复中途被推翻的旧理解。
- 对于 CSV 内容中显示乱码的问题，仅视为当前读取展示的编码现象；不影响字段级关系复述。
- 不在本次回复里重复敏感数据库凭据，只保留与关系说明有关的表名与字段映射。
- `shops.csv` 在本次复述中定位为“正式实时看板采集配置与展示补充元数据来源”，不与 `shops_name.csv` 的主连接角色混淆。

## Verification Steps

- 检查最终复述是否完整覆盖 5 个对象的职责。
- 检查是否明确说明了主连接键 `companyshop_name` 的作用。
- 检查是否明确区分了：
  - `shops_name.csv` 的主表角色
  - `shops.csv` 的采集配置角色
  - `target.csv` 的目标角色
  - `to_date.csv` 的导航与周期控制角色
  - MySQL 的历史单日数据角色
- 检查是否明确区分“实时看板”和“周期看板”的数据来源差异。
