# 整改前基线报告

> 建立时间：2026-05-08 16:46  
> 基线用途：整改前后对比的参照锚点  
> 所有检查均在不修改代码的前提下完成  

---

## 基线汇总

| 检查项 | 结果 | 说明 |
|---|---|---|
| Git 状态 | ❌ 不是仓库 | `fatal: not a git repository`，`.git/` 不存在 |
| 当前分支 | N/A | 无版本控制 |
| 未提交修改 | N/A | 无版本控制 |
| 未跟踪文件 | N/A | 全部文件未被跟踪 |
| 备份状态 | ✅ 双份 | `项目目录_backup` + `%TEMP%\GMV-LiveLens_backup_20260508` |
| `.gitignore` | ✅ 存在 | 2026-05-08 新建（上一轮 P1 修复） |
| `.env.example` | ✅ 存在 | 2026-05-08 新建（上一轮 P1 修复） |
| full_test 执行结果 | ✅ 47/47 PASS (100%) | 模块导入 + 业务逻辑 + DB CRUD + OCR 管道 + 前端检查 |
| smoke_api 执行结果 | ✅ 14/14 PASS (100%) | 含服务健康、任务、Edge 会话、OCR、WS、前端主页 |
| 端口 8100 | ✅ 空闲 | 测试结束后已释放 |
| 当前是否适合开始整改 | ✅ 是 | 基线已锁定，备份额确认，所有测试通过 |

---

## 测试详情

### Section A: 模块导入 + Bug 回归 (10/10)
全部通过。scheduler.logger 已定义、_run_loop 含异常保护、next_edge_debug_port 含上界。

### Section B: 业务逻辑 (18/18)
金额解析、OCR 候选提取、形近字纠错、_judge 状态机边界全通过。

### Section C: 数据库 CRUD (10/10)
临时数据库 init_db、upsert_task、add_sample、settings 读写、端口分配全部正确。

### Section D: OCR 管道 (4/4)
RapidOCR + ddddocr 可用，合成图片 474487 正确识别，万/亿单位正确。

### Section E: API 集成 (跳过，需服务运行)
→ 改为独立运行 smoke_api.py：14/14 PASS。

### Section F: 前端检查 (5/5)
7 个必需文件齐全、JS 加载顺序正确、WS 端点存在、settings 接口引用存在、无硬编码端口。

---

## 关键指标

| 指标 | 基线值 |
|---|---|
| Python 版本 | 3.14.3 |
| 可用 OCR 引擎 | RapidOCR + ddddocr |
| 店铺数量 | 9 家（shops.csv GBK 编码） |
| 启动方式 | `第1步_启动GMV服务.bat` / `uvicorn backend.main:app --port 8100` |
| 前端入口 | `frontend/index.html` (原生 JS, 5 模块) |
| 数据库 | SQLite (`data/gmv_livelens.sqlite3`) |
| 测试入口 | `backend/tools/full_test.py` + `backend/tools/smoke_api.py` |

---

## 待整改 P1 问题（本基线锚定）

| 编号 | 问题 | 所处文件 |
|---|---|---|
| P1-1 | 无 lint/formatter 配置 | 全局 |
| P1-2 | main.py 2347 行未拆分路由 | `backend/main.py` |
| P1-3 | remote_edge.py 3072 行 | `backend/collectors/remote_edge.py` |
| P1-4 | 无 Docker 支持 | 全局 |
| P1-5 | 无 CI/CD | 全局 |

---

## 结论

| 判断 | 结论 |
|---|---|
| **是否可以开始第一阶段低风险整改** | ✅ 是 |
| **是否需要先备份** | ✅ 已确认双份备份存在 |
| **是否存在不能继续的风险** | ❌ 不存在—所有测试通过，备份完整 |
| **下一步应该执行什么** | 第一阶段低风险整改：ruff 配置、依赖锁定、日志、测试迁移 |

```
┌──────────────────────────────────────────────┐
│  基线锚点 2026-05-08 16:46                   │
├──────────────────────────────────────────────┤
│  full_test  : 47/47 PASS ✅                  │
│  smoke_api  : 14/14 PASS ✅                  │
│  服务状态   : 正常启动/停止 ✅               │
│  备份       : 双份 ✅                        │
│  Git        : 未初始化（待 init）            │
│  风险判定   : 无阻塞，可开始整改             │
└──────────────────────────────────────────────┘
```

---

> **基线确认人：AI 技术负责人**  
> **日期：2026-05-08 16:46**
